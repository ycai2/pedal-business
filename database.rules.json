{
  "rules": {
    "users": {
      "$uid": {
        ".read": true,
        ".write": "$uid === auth.uid",
        "specials": {
          ".read": true,
          ".write": "$uid === auth.uid"
        },
        "profile_info": {
          ".read": true,
          ".write": "$uid === auth.uid"
        }
      }
    }
  }
}