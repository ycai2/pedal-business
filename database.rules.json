{
  "rules": {
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid",
        "events": {
          ".read": true,
          ".write": "$uid === auth.uid"
        },
        "profile_info": {
          ".read": true,
          ".write": "$uid === auth.uid"
        }
      }
    }
  }
}